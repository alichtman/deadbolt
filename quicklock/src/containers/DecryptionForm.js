import React, { Component } from "react";
import "./DecryptionForm.css";

export default class DecryptionForm extends Component {
  render() {
    const { fileName, onDecrypt, onAbort } = this.props;

    return <div></div>;
  }
}
